{{ header }}

<div class="BT">
    <h1>Welcome To Big Time</h1>

    {{> form }}
    
    <div class="Burgers">
        {{#each burgers}}
            {{> getBurger }}
        {{/each}}
    </div>
</div>
<div>
    <div class="container">
        <h1>Welcome To Big Time</h1>
    </div>

    {{> form }}
    
    <div class="Burgers">
        {{#each burgers}}
            {{> getBurger }}
        {{/each}}
    </div>
</div>